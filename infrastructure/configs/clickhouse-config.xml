<?xml version="1.0"?>
<clickhouse>
    <!-- Single-node ClickHouse configuration for PostHog -->
    <!-- Removed cluster configuration for single-instance setup -->

    <!-- Required for PostHog single-node -->
    <profiles>
        <default>
            <max_memory_usage>10000000000</max_memory_usage>
            <use_uncompressed_cache>0</use_uncompressed_cache>
            <load_balancing>random</load_balancing>
        </default>
    </profiles>

    <!-- Single-node setup - no macros needed -->
    <!-- Remove macros section for single-instance ClickHouse -->

</clickhouse>
