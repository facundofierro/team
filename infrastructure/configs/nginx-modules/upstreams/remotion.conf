# Remotion service upstream configuration
# Use variable to allow runtime resolution instead of startup resolution
# Commented out to prevent startup DNS resolution failures
# upstream remotion {
#     server remotion:3001 max_fails=3 fail_timeout=30s;
#     # Add a backup directive to prevent nginx from failing completely
#     server 127.0.0.1:3001 backup down;
#     keepalive 2;
# }
